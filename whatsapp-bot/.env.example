# Firebase Realtime Database URL (from Firebase Console → Project Settings → General)
FIREBASE_DB_URL=https://YOUR-PROJECT-default-rtdb.firebaseio.com

# Optional: Twilio auth validation (recommended for production)
TWILIO_AUTH_TOKEN=your_twilio_auth_token
